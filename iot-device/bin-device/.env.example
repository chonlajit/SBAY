# ==========================================
# SBAY Smart Bin - Environment Configuration
# ให้ก็อปปี้ไฟล์นี้ไปเปลี่ยนชื่อเป็น .env และแก้ไขค่าด้านล่าง
# ==========================================

# --- การเชื่อมต่อ Backend ---
# (จำเป็นต้องใส่) รหัสลับสำหรับยืนยันตัวตนของตู้นี้
DEVICE_SECRET=your_secret_key_here

# (จำเป็นต้องใส่) URL ของเซิร์ฟเวอร์ Backend
BACKEND_URL=http://localhost:8070

# --- ข้อมูลประจำตู้ ---
# รหัสประจำตู้ (ถ้าไม่ใส่จะใช้ค่าเริ่มต้น BIN-001)
DEVICE_ID=BIN-001

# ชื่อตู้ที่จะแสดงใน Dashboard (เช่น "ตู้หน้าคณะวิศวะ")
DEVICE_NAME=Smart Bin

# สถานที่ตั้งตู้ที่จะแสดงใน Dashboard (เช่น "ชั้น 1 อาคาร A")
DEVICE_LOCATION=SBAY Location

# --- โหมดการทำงาน (Hardware & Features) ---
# ตั้งเป็น true หรือ false เพื่อเปิด/ปิดการใช้งานฟีเจอร์ต่างๆ
USE_HARDWARE=true
USE_CAMERA=true
USE_GUI=true
USE_IR=true
USE_SERVO=true
USE_RESET_BUTTONS=true

# --- การตั้งค่าองศา Servo (Calibration) ---
# คุณสามารถตั้งค่าเริ่มต้นให้ศูนย์กลาง (DEFAULT) และองศาการปัดของแต่ละประเภทขยะได้ที่นี่เลย
DEFAULT_SORT_ANGLE=260
DEFAULT_RELEASE_ANGLE=82

SORT_ANGLE_PLASTIC=260
SORT_ANGLE_CAN=200
SORT_ANGLE_CARTON=320

RELEASE_ANGLE_PLASTIC=104
RELEASE_ANGLE_CAN=60
RELEASE_ANGLE_CARTON=60

# --- Servo Angles (สำหรับมอเตอร์ 180 องศา - Drop & Return) ---
DROP_ANGLE_CLOSED=90
DROP_ANGLE_OPEN=180

RETURN_ANGLE_CLOSED=90
RETURN_ANGLE_OPEN=180

# --- AI Model ---
# ชื่อไฟล์โมเดล YOLO ที่วางอยู่ในโฟลเดอร์ bin-device
MODEL_PATH=yolo.pt
