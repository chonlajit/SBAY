FROM node:20-alpine
WORKDIR /app

# Install dependencies
COPY package.json package-lock.json ./
RUN npm install --legacy-peer-deps

# Copy source (though strictly we rely on volume mount, this is a fallback)
COPY . .

# Next.js Dev Port
EXPOSE 3000

# Start in Dev mode
CMD ["npm", "run", "dev"]
